"""Web dashboard package: serves static UI and a live WebSocket stream.

Submodules:
* ``server`` - the HTTP + WebSocket dashboard server.
* ``ws``     - minimal RFC 6455 server implementation (stdlib only).
"""
